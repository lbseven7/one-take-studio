{
  "name": "take-um-studio",
  "private": true,
  "description": "Take Um Studio — testes",
  "scripts": {
    "test": "node --test",
    "test:supabase": "node tests/supabase-smoke.js",
    "deploy": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/deploy.ps1"
  }
}
